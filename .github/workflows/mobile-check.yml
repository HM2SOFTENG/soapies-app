name: Mobile App Check (legacy shim)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Use .github/workflows/mobile-ci.yml for the canonical Spoapies mobile CI workflow."
